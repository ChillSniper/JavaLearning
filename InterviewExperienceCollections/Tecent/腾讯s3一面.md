# The Interviews of Tecent S3 Department From Group's friends

##

### 一、 个人背景与项目经历

- 请先自我介绍一下。
- 你不是计算机专业的是吧？你本科的“机器人工程”是一个什么样的专业（偏机械还是自动化）？
- 怎么想到跨专业去学生物医学工程的？
- 简历上的两个项目都是自己独立开发的吗？
- 项目的前端是用现成的（教程里的）还是自己做的？
- 介绍一下“本地生活”项目，以及你觉得其中比较有挑战、有技术亮点的地方。

### 二、 Redis 与缓存架构

- 解决缓存双写一致性（强一致性与最终一致性）的方案是什么样的？
- 如果先改数据库然后删缓存，在删缓存之前有人读了缓存的数据，读到的不就是错的数据了吗？这个时候怎么处理？
- 缓存击穿和缓存穿透是什么场景？
- 缓存穿透怎么解决？
- 有没有了解过布隆过滤器？讲一下布隆过滤器是怎么防止缓存穿透的？
- 有没有用过Redis的其他数据结构？
- Zset和Set有什么区别？
- Zset的底层原理有了解过吗？跳表是一个什么样的结构？
- 跳表查询为什么快？跳表插入数据是什么样一个流程？
- 基于Redis+AOP+自定义注解的滑动窗口限流算法是怎么实现的？
- 如何保证限流操作中Redis多步命令的一致性（原子性）？
- 为什么Redis通过Lua脚本能够保证原子性？

### 三、 MySQL 数据库

- 数据库有哪些事务隔离级别？这四个隔离级别分别解决了什么问题？
- 脏读和幻读是什么问题？
- MySQL是怎么解决幻读这个问题的？
- MVCC（多版本并发控制）机制了解过吗？它是怎么一个机制？
- InnoDB的索引实现原理了解过吗？能讲一下B+树的数据结构吗？

### 四、 JVM 与 Java 集合

- JVM的内存模型有了解过吗？
- 如果new一个对象，它的内存分配在哪个地方？
- 堆是怎么划分的？新生代可以怎么划分？
- 经历一次垃圾回收算法后，新生代里的对象会发生什么变化？
- 有没有了解过一些垃圾回收算法？新生代使用的是什么算法？
- 什么样的对象是可以被回收的对象？什么样的对象可以作为GC Root对象？
- 声明一个基本类型（如int数值），它会分配在哪个地方？
- 引用类型有了解过吗？
- 如果new一个Integer 1，它和数字1是相等的吗？
- HashMap的原理有了解过吗？是怎么扩容的？为什么扩容是扩两倍？

### 五、 Java 并发编程

- `Synchronized`关键字有了解过吗？它的用法是什么？
- 有没有了解过它的底层原理？
- 锁升级是一个什么样的逻辑？
- 偏向锁是怎么实现的？线程ID存在哪个地方？

### 六、 算法题与反问

- 代码题：最长无重复子串。
- 反问环节：部门业务及技术栈（负责招聘平台，java为主）
